Sp.LogView = Em.View.extend({
    templateName: 'log',

    position: null,
    background: "bg"

    /*classNamesStr: function() {
        return "config " + this.get("background") + " " + this.get("position") ;
    }.property('background')*/

/*    didInsertElement: function() {
        console.log("config did render") ;

        this.controller.switchMenuItem("configuration") ;
    }*/
});
